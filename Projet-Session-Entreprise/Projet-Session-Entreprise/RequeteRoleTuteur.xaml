<Window x:Class="Projet_Session_Entreprise.RequeteRoleTuteur"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet_Session_Entreprise"
        mc:Ignorable="d"
        Title="RequeteRoleTuteur" Height="450" Width="800">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Teacher Manager"
            FontSize="26"
            FontWeight="Bold"
            Foreground="#2C3E50"
            Grid.Column="1"
            TextAlignment="Center"/>

        <TextBlock Margin="0,43,0,15"
            Text="Devenir tuteur"
            FontSize="20"
            FontWeight="Medium"
            Foreground="#2C3E50"
            Grid.Column="1"
            TextAlignment="Center"/>

        <!-- faut encore le data binding -->
        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="10">
                <TextBlock Text="Cours choisi" FontWeight="SemiBold"/>
                <TextBox x:Name="txtClass"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <Button x:Name="btnSendRequest" 
                    Width="120" Height="30" 
                    Content="Faire une demande" 
                    Margin="0 15"                         
                    Background="#2ECC71"
                    Foreground="White"
                    FontWeight="Bold"/>
            </StackPanel>

        </Grid>
    </Grid>
</Window>
