<UserControl x:Class="Project_Session_Entreprise.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             Margin="20"
             Background="White">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Profil"
                   FontSize="20"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="DA :" Margin="5"/>
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 x:Name="NumDA"
                 Text="{Binding DA}"
                 IsReadOnly="True"
                 Margin="5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Nom :" Margin="5"/>
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 x:Name="TxtNom"
                 Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Prénom :" Margin="5"/>
        <TextBox Grid.Row="3"
                 Grid.Column="1"
                 x:Name="TxtPrenom"
                 Text="{Binding Prenom, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Rôle :" Margin="5"/>
        <TextBox Grid.Row="4"
                 Grid.Column="1"
                 x:Name="TxtRole"
                 Text="{Binding Role, UpdateSourceTrigger=PropertyChanged}"
                 Margin="5"/>

        <Button Grid.Row="5"
                Grid.ColumnSpan="2"
                Content="Enregistrer"
                x:Name="BtnEnregistrer"
                Width="100"
                Margin="5"
                HorizontalAlignment="Left"/>

    </Grid>
</UserControl>