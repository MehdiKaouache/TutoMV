<UserControl x:Class="Projet_Session_Entreprise.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Recherche de tuteurs" FontSize="32" FontWeight="Bold" Foreground="{StaticResource MainText}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,25">
            <TextBox Width="400" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0"/>
            <Button Content="Rechercher" Style="{StaticResource ActionButton}" Width="150" Command="{Binding RechercherCommand}"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid x:Name="dgResults" ItemsSource="{Binding Results}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="NOM" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTextColumn Header="PRÉNOM" Binding="{Binding Prenom}" Width="*"/>
                    <DataGridTextColumn Header="MATIÈRE" Binding="{Binding Subject}" Width="*"/>
                    <DataGridTextColumn Header="DISPONIBILITÉ" Binding="{Binding Availability}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>