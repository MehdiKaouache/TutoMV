<UserControl x:Class="Projet_Session_Entreprise.Views.RoleSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource MainBG}">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="TutoMV" FontSize="42" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Sélectionnez votre type de profil pour continuer" FontSize="16" Foreground="{StaticResource SubText}" HorizontalAlignment="Center" Margin="0,0,0,50"/>

            <StackPanel Orientation="Horizontal">
                <Border x:Name="CardStudent" Style="{StaticResource CardStyle}" Width="380" Height="280" Margin="20" Cursor="Hand" MouseLeftButtonDown="SelectStudent_Click">
                    <Grid>
                        <Border Background="{StaticResource PrimaryMauve}" Height="4" VerticalAlignment="Top" CornerRadius="10,10,0,0" Margin="-25,-25,-25,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ÉTUDIANT" FontSize="12" FontWeight="Black" Foreground="{StaticResource PrimaryMauve}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <TextBlock Text="Portail Apprenant" FontSize="26" FontWeight="Bold" Foreground="{StaticResource MainText}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Trouvez un tuteur et gérez vos séances" FontSize="14" Foreground="{StaticResource SubText}" HorizontalAlignment="Center" Margin="0,15,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border x:Name="CardTutor" Style="{StaticResource CardStyle}" Width="380" Height="280" Margin="20" Cursor="Hand" MouseLeftButtonDown="SelectTutor_Click">
                    <Grid>
                        <Border Background="{StaticResource SuccessGreen}" Height="4" VerticalAlignment="Top" CornerRadius="10,10,0,0" Margin="-25,-25,-25,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ENSEIGNANT" FontSize="12" FontWeight="Black" Foreground="{StaticResource SuccessGreen}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <TextBlock Text="Portail Tuteur" FontSize="26" FontWeight="Bold" Foreground="{StaticResource MainText}" HorizontalAlignment="Center"/>
                            <TextBlock Text="Partagez vos connaissances et gérez vos revenus" FontSize="14" Foreground="{StaticResource SubText}" HorizontalAlignment="Center" Margin="0,15,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <Button Content="Retour à la connexion" Style="{StaticResource LinkButton}" Margin="0,40,0,0" Click="BackToLogin_Click"/>
        </StackPanel>
    </Grid>
</UserControl>