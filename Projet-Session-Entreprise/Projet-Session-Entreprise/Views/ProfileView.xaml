<UserControl x:Class="Projet_Session_Entreprise.UI.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Projet_Session_Entreprise.UI.Views"
             Background="{StaticResource MainBG}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Mon Profil" FontSize="36" FontWeight="ExtraBold" Foreground="{StaticResource PrimaryMauve}"/>
            <Border Height="3" Background="{StaticResource PrimaryMauve}" Width="60" HorizontalAlignment="Left" Margin="2,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Informations Personnelles" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}" Margin="0,0,0,25"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Prénom" Foreground="{StaticResource SubText}" FontSize="12"/>
                                <TextBlock Text="{Binding Prenom}" FontSize="18" Foreground="White"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Nom" Foreground="{StaticResource SubText}" FontSize="12"/>
                                <TextBlock Text="{Binding Nom}" FontSize="18" Foreground="White"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Numéro de DA" Foreground="{StaticResource SubText}" FontSize="12"/>
                        <TextBlock Text="{Binding DA}" FontSize="18" Foreground="White" Margin="0,0,0,15"/>

                        <StackPanel Visibility="{Binding IsStudent, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Cote R / GPA" Foreground="{StaticResource SubText}" FontSize="12"/>
                            <TextBlock Text="{Binding Gpa}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessGreen}" Margin="0,0,0,15"/>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsTutor, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="Matière enseignée" Foreground="{StaticResource SubText}" FontSize="12"/>
                            <TextBlock Text="{Binding Matiere}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>

                            <TextBlock Text="Statut" Foreground="{StaticResource SubText}" FontSize="12"/>
                            <TextBlock Text="{Binding StatutValidation}" FontSize="16" FontWeight="SemiBold" Foreground="#FBC531" Margin="0,0,0,15"/>
                        </StackPanel>

                        <Border Background="#2D2D2D" CornerRadius="8" HorizontalAlignment="Left" Padding="15,8" Margin="0,10,0,0">
                            <TextBlock Text="{Binding Role}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}"/>
                        </Border>

                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Aperçu de l'activité" FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}" Margin="0,0,0,20"/>
                        <UniformGrid Columns="3">
                            <StackPanel>
                                <TextBlock Text="Séances" Foreground="{StaticResource SubText}" FontSize="12" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding TotalSeances}" FontSize="28" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Heures" Foreground="{StaticResource SubText}" FontSize="12" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding HeuresCompletees}" FontSize="28" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>
                            <StackPanel Visibility="{Binding IsTutor, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="Note (/5)" Foreground="{StaticResource SubText}" FontSize="12" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding MoyenneStats}" FontSize="28" FontWeight="Bold" Foreground="#FFD700"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Grid x:Name="AvailabilityContainer" Grid.Column="2" Visibility="{Binding IsTutor, Converter={StaticResource BoolToVis}}">
                <Border Style="{StaticResource CardStyle}" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <local:TutorAvailabilityControlView x:Name="ctrlAvailability" Grid.Row="1"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>