<UserControl x:Class="Projet_Session_Entreprise.UI.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Projet_Session_Entreprise.UI.Views"
             Background="{StaticResource MainBG}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="40">
            <StackPanel MaxWidth="900" HorizontalAlignment="Left">

                <Border Background="#161625" CornerRadius="15" Padding="30" Margin="0,0,0,30" BorderBrush="#2A2A4A" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="100" Height="100" CornerRadius="50" Background="{StaticResource PrimaryMauve}" Margin="0,0,25,0">
                            <TextBlock Text="👤" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Role, StringFormat='Profil {0}'}" FontSize="14" Foreground="{StaticResource PrimaryMauve}" FontWeight="Bold" Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="{Binding Prenom}" FontSize="32" FontWeight="ExtraBold" Foreground="White" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding Nom}" FontSize="32" FontWeight="ExtraBold" Foreground="White"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Numéro DA :" Foreground="{StaticResource SubText}" FontSize="15" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding DA}" Foreground="White" FontSize="15" FontWeight="Bold"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="#201A35" BorderBrush="{StaticResource PrimaryMauve}" BorderThickness="1" CornerRadius="12" Padding="25" Margin="0,0,0,30" Visibility="{Binding CanEditGpa, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="👨‍🏫 Évaluation Académique" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                        <TextBlock Text="En tant que tuteur de cet élève, vous pouvez mettre à jour sa moyenne générale suite à vos séances." Foreground="{StaticResource SubText}" FontSize="14" Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Nouvelle moyenne (%) :" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="15"/>
                            <TextBox Grid.Column="1" Text="{Binding NewGpaInput, UpdateSourceTrigger=PropertyChanged}" Height="45" Style="{StaticResource ModernTextBoxStyle}" MaxWidth="150" HorizontalAlignment="Left" FontSize="16" TextAlignment="Center"/>
                            <Button Grid.Column="2" Content="Sauvegarder" Command="{Binding UpdateStudentGpaCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="45" Width="140" Margin="15,0,0,0"/>
                        </Grid>
                        <TextBlock Text="{Binding GpaStatusMessage}" Foreground="#00E676" FontWeight="Bold" Margin="0,15,0,0"/>
                    </StackPanel>
                </Border>

                <StackPanel Visibility="{Binding IsStudent, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Résumé Académique" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Moyenne Actuelle" FontSize="15" Foreground="{StaticResource SubText}"/>
                                <TextBlock Text="{Binding Gpa, StringFormat='{}{0} %'}" FontSize="36" FontWeight="Bold" Foreground="#FBC531" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Séances Complétées" FontSize="15" Foreground="{StaticResource SubText}"/>
                                <TextBlock Text="{Binding TotalSeances}" FontSize="36" FontWeight="Bold" Foreground="White" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="4" Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Heures de tutorat" FontSize="15" Foreground="{StaticResource SubText}"/>
                                <TextBlock Text="{Binding HeuresCompletees, StringFormat='{}{0}h'}" FontSize="36" FontWeight="Bold" Foreground="#00E676" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Button Content="🔍 Trouver un tuteur" Style="{StaticResource PrimaryButtonStyle}" Height="50" Width="250" HorizontalAlignment="Left" FontSize="16" Click="SearchTutor_Click"/>
                </StackPanel>

                <StackPanel Visibility="{Binding IsTutor, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Expertise &amp; Impact" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

                    <Border Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Matières enseignées" FontSize="15" Foreground="{StaticResource SubText}" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding Matiere}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Statut" FontSize="15" Foreground="{StaticResource SubText}"/>
                                <TextBlock Text="{Binding StatutValidation}" FontSize="20" FontWeight="Bold" Foreground="#00E676" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Séances Données" FontSize="15" Foreground="{StaticResource SubText}"/>
                                <TextBlock Text="{Binding TotalSeances}" FontSize="36" FontWeight="Bold" Foreground="White" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="4" Background="#161625" CornerRadius="12" Padding="20" BorderBrush="#2A2A4A" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Note Moyenne" FontSize="15" Foreground="{StaticResource SubText}"/>
                                <TextBlock Text="{Binding MoyenneStats}" FontSize="36" FontWeight="Bold" Foreground="#FBC531" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <local:TutorAvailabilityControlView x:Name="ctrlAvailability" Margin="0,0,0,20"/>

                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>