<Window x:Class="Projet_Session_Entreprise.EditCompteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projet_Session_Entreprise"
        mc:Ignorable="d"
        Title="Modifier le Compte" Height="450" Width="800">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2.1*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Teacher Manager"
           FontSize="26"
           FontWeight="Bold"
           TextAlignment="Center"
           Foreground="#2C3E50"
           Background="WhiteSmoke"
           Margin="0 0 0 0"/>

        <TextBlock Text="Votre compte"
            Grid.Column="1"
            TextAlignment="Center"
            FontSize="26"
            FontWeight="Medium"
            Foreground="#2C3E50"
            Margin="0 0 0 0"/>

        <TextBlock Text="Menu (à ajouter)"
            Grid.Row="1"
            FontSize="26"
            FontWeight="Light"
            TextAlignment="Center"
            Foreground="#2C3E50"
            Background="WhiteSmoke"
            Margin="0 0 0 0"/>
        
        <Grid Grid.Row="1" Grid.Column="1">
            <!-- faut encore le data binding -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
        </Grid>
    </Grid>
</Window>
