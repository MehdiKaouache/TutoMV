<UserControl x:Class="Projet_Session_Entreprise.Views.ReceivedRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MainBG}">
    <UserControl.Resources>
        <Style x:Key="NoSelectionVisualsStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Demandes reçues" FontSize="36" FontWeight="ExtraBold" Foreground="{DynamicResource PrimaryMauve}"/>
            <Border Height="3" Background="{DynamicResource PrimaryMauve}" Width="60" HorizontalAlignment="Left" Margin="2,5,0,0"/>

            
            <TextBlock Text="{Binding NotificationMessage}"
                       Foreground="{DynamicResource SuccessGreen}"
                       FontWeight="Bold"
                       Margin="0,10,0,0"/>

            
            <ProgressBar Height="6"
                         IsIndeterminate="True"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                         Margin="0,10,0,0"/>
        </StackPanel>

        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding Appointments}" 
                 Background="Transparent" 
                 BorderThickness="0"
                 ItemContainerStyle="{DynamicResource NoSelectionVisualsStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,15" Width="600" HorizontalAlignment="Left">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{Binding DateRDV, StringFormat={}{0:dd MMMM yyyy à HH:mm}}" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource MainText}"/>
                                <TextBlock Text="{Binding Status}" Foreground="{DynamicResource SubText}" Margin="0,5,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Accepter" Style="{DynamicResource ActionButton}" Width="100" Height="35" Margin="0,0,10,0"
                                        Command="{Binding DataContext.AccepterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="Refuser" Style="{DynamicResource ActionButton}" Background="{DynamicResource DangerRed}" Width="100" Height="35"
                                        Command="{Binding DataContext.RefuserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>