<UserControl x:Class="Projet_Session_Entreprise.Views.ReceivedRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource MainBG}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Demandes reçues" FontSize="36" FontWeight="ExtraBold" Foreground="{StaticResource PrimaryMauve}"/>
            <Border Height="3" Background="{StaticResource PrimaryMauve}" Width="60" HorizontalAlignment="Left" Margin="2,5,0,0"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="LightGreen" FontSize="14" Margin="0,15,0,0" FontWeight="Bold"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <DataGrid x:Name="dgRequests" ItemsSource="{Binding Appointments}" 
                      AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" Foreground="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding DateRDV, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="*"/>
                    <DataGridTextColumn Header="Durée" Binding="{Binding Duree, TargetNullValue='Non spécifiée'}" Width="150"/>
                    <DataGridTextColumn Header="Lieu" Binding="{Binding Lieu, TargetNullValue='Non spécifié'}" Width="200"/>

                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✓" Command="{Binding DataContext.AccepterCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Green" Foreground="White" Width="30" Margin="0,0,5,0"/>
                                    <Button Content="✕" Command="{Binding DataContext.RefuserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Red" Foreground="White" Width="30"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>