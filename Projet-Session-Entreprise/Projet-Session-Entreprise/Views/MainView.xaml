<Window x:Class="Projet_Session_Entreprise.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Projet_Session_Entreprise.Views"
        Title="TutoMV" Height="850" Width="1300" Background="{StaticResource MainBG}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarCol" Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:NavbarControlView x:Name="TopNavbar" Grid.Row="0" Grid.ColumnSpan="2" Visibility="Collapsed"/>
        <local:SidebarControlView x:Name="LeftSidebar" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Visibility="Visible"/>

        <Grid Grid.Row="1" Grid.Column="1" Margin="35">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border x:Name="NotificationBanner" Grid.Row="0" Background="#2D5A27" CornerRadius="8" Padding="15" Margin="0,0,0,20" Visibility="Collapsed">
                <TextBlock x:Name="NotificationText" Foreground="White" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
            </Border>

            <ContentControl x:Name="MainContentArea" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>