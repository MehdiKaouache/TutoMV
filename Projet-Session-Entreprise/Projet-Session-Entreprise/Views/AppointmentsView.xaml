<UserControl x:Class="Projet_Session_Entreprise.UI.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource MainBG}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Mes Rendez-vous" FontSize="36" FontWeight="ExtraBold" Foreground="{StaticResource PrimaryMauve}"/>
            <Border Height="3" Background="{StaticResource PrimaryMauve}" Width="60" HorizontalAlignment="Left" Margin="2,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ListBox Grid.Column="0" Margin="0,0,20,0" Background="Transparent" BorderThickness="0" 
                     ItemsSource="{Binding MyAppointments}" 
                     SelectedItem="{Binding SelectedAppointment}" 
                     ItemContainerStyle="{StaticResource CleanListBoxItem}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListCardStyle}">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding DateRDV, StringFormat={}{0:dd MMMM yyyy à HH:mm}}" FontWeight="Bold" FontSize="18" Foreground="White" Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <Border Background="#2D2D2D" CornerRadius="5" Padding="8,4" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding Status}" Foreground="{StaticResource PrimaryMauve}" FontSize="12" FontWeight="Bold"/>
                                    </Border>
                                    <TextBlock Text="{Binding Duree, TargetNullValue='Durée non spécifiée'}" Foreground="{StaticResource SubText}" VerticalAlignment="Center" FontSize="13"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Column="1" Visibility="{Binding DetailsVisibility}">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Détails du rendez-vous" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}" Margin="0,0,0,15"/>

                        <TextBlock Text="Date et Heure" Foreground="{StaticResource SubText}" FontSize="12"/>
                        <TextBlock Text="{Binding SelectedAppointment.DateRDV, StringFormat={}{0:dd MMMM yyyy à HH:mm}}" FontSize="16" Foreground="White" Margin="0,0,0,15"/>

                        <TextBlock Text="Durée estimée" Foreground="{StaticResource SubText}" FontSize="12"/>
                        <TextBlock Text="{Binding SelectedAppointment.Duree, TargetNullValue='Non spécifiée'}" FontSize="16" Foreground="White" Margin="0,0,0,15"/>

                        <TextBlock Text="Lieu / Lien visio" Foreground="{StaticResource SubText}" FontSize="12"/>
                        <TextBlock Text="{Binding SelectedAppointment.Lieu, TargetNullValue='Non spécifié'}" FontSize="16" Foreground="White" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}" Visibility="{Binding ReviewSectionVisibility}">
                    <StackPanel>
                        <TextBlock Text="Noter la séance" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryMauve}" Margin="0,0,0,15"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="Note (/5) :" VerticalAlignment="Center" Foreground="{StaticResource SubText}" Margin="0,0,15,0"/>
                            <TextBox Text="{Binding Rating}" Width="60" Style="{StaticResource ModernTextBoxStyle}" TextAlignment="Center" FontSize="16" FontWeight="Bold"/>
                        </StackPanel>
                        <TextBlock Text="Commentaire :" Foreground="{StaticResource SubText}" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Comment}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15" VerticalContentAlignment="Top"/>
                        <Button Content="Envoyer l'avis" Command="{Binding AddReviewCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessGreen}" Margin="0,15,0,0" TextWrapping="Wrap" FontWeight="SemiBold" TextAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>