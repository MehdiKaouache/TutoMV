<UserControl x:Class="Projet_Session_Entreprise.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource MainBG}">
    <Grid>
        <Border Style="{StaticResource CardStyle}" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="TutoMV" Foreground="{StaticResource PrimaryMauve}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Connectez-vous" Foreground="{StaticResource SubText}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Numéro DA" Foreground="{StaticResource MainText}" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDA"/>

                <TextBlock Text="Mot de passe" Foreground="{StaticResource MainText}" Margin="0,15,0,5"/>
                <Grid>
                    <PasswordBox x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged"/>
                    <TextBox x:Name="txtVisiblePassword" Visibility="Collapsed" TextChanged="txtVisiblePassword_TextChanged"/>
                    <Button x:Name="btnTogglePassword" Content="👁" Width="30" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="{StaticResource SubText}" Cursor="Hand" Click="btnTogglePassword_Click"/>
                </Grid>

                <TextBlock x:Name="txtError" Foreground="#ff4444" Margin="0,10,0,0" TextWrapping="Wrap" HorizontalAlignment="Center" Visibility="Collapsed"/>

                <Button Content="Se connecter" Style="{StaticResource ActionButton}" Margin="0,30,0,10" Click="BtnLogin_Click"/>
                <Button Content="Créer un compte" Style="{StaticResource LinkButton}" Click="BtnRegister_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>