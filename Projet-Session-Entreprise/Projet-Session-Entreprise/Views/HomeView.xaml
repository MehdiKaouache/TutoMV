<UserControl x:Class="Projet_Session_Entreprise.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel x:Name="GuestPanel" Visibility="Collapsed">
            <TextBlock Text="Bienvenue sur TutoMV" FontSize="42" FontWeight="Bold" Foreground="{DynamicResource PrimaryMauve}" Margin="0,0,0,10"/>
            <TextBlock Text="La plateforme de tutorat n°1 pour réussir vos examens." FontSize="18" Foreground="{DynamicResource SubText}" Margin="0,0,0,40"/>
            <Border Style="{DynamicResource CardStyle}" Padding="40">
                <StackPanel>
                    <TextBlock Text="Pourquoi choisir TutoMV ?" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                    <TextBlock Text="• Tuteurs certifiés par l'école" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="• Réservation simple et rapide" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <Button Content="Créer un compte maintenant" Style="{DynamicResource ActionButton}" Width="250" HorizontalAlignment="Left" Click="btnRegisterGuest_Click" Margin="0,20,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel x:Name="StudentPanel" Visibility="Collapsed">
            <TextBlock Text="Tableau de bord - Étudiant" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource MainText}"/>
            <UniformGrid Columns="3" Margin="0,20,0,30">
                <Border Style="{DynamicResource CardStyle}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Séances suivies" Foreground="{DynamicResource SubText}" FontSize="12"/>
                        <TextBlock Text="4" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryMauve}" Margin="0,8"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource CardStyle}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Heures complétées" Foreground="{DynamicResource SubText}" FontSize="12"/>
                        <TextBlock Text="12.5h" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource SuccessGreen}" Margin="0,8"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Moyenne" Foreground="{DynamicResource SubText}" FontSize="12"/>
                        <TextBlock Text="4.9/5" FontSize="28" FontWeight="Bold" Foreground="#FBC531" Margin="0,8"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
            <Border Background="{DynamicResource PrimaryMauve}" CornerRadius="15" Padding="35">
                <StackPanel>
                    <TextBlock Text="Prêt pour votre prochaine réussite ?" FontSize="22" FontWeight="Bold" Foreground="#000000"/>
                    <Button Content="Explorer les tuteurs" Width="200" HorizontalAlignment="Left" Style="{DynamicResource ActionButton}" Background="Black" Foreground="White" Click="btnExplore_Click" Margin="0,20,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel x:Name="TutorPanel" Visibility="Collapsed">
            <TextBlock Text="Tableau de bord - Tuteur" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource MainText}"/>
            <UniformGrid Columns="3" Margin="0,20,0,30">
                <Border Style="{DynamicResource CardStyle}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Étudiants aidés" Foreground="{DynamicResource SubText}" FontSize="12"/>
                        <TextBlock Text="18" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryMauve}" Margin="0,8"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource CardStyle}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="Heures de tutorat" Foreground="{DynamicResource SubText}" FontSize="12"/>
                        <TextBlock Text="24h" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource SuccessGreen}" Margin="0,8"/>
                    </StackPanel>
                </Border>
                <Border Style="{DynamicResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Note" Foreground="{DynamicResource SubText}" FontSize="12"/>
                        <TextBlock Text="4.9/5" FontSize="28" FontWeight="Bold" Foreground="#FBC531" Margin="0,8"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
            <Border Style="{DynamicResource CardStyle}" BorderBrush="{DynamicResource PrimaryMauve}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Demandes en attente" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <Button Content="Gérer mes séances" Style="{DynamicResource ActionButton}" Width="200" Margin="0,20,0,0" Click="btnManageRequests_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>