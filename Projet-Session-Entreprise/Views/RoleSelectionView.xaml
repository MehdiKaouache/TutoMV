<Window x:Class="Projet_Session_Entreprise.Views.RoleSelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="S'inscrire" Height="180" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Vous êtes :" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Width="220">
            <Button x:Name="btnStudent" Content="Étudiant" Height="36" Margin="0,0,0,8"/>
            <Button x:Name="btnTeacher" Content="Enseignant" Height="36"/>
        </StackPanel>
    </Grid>
</Window>
