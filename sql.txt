DROP DATABASE IF EXISTS schooldb;
CREATE DATABASE schooldb;
USE schooldb;

CREATE TABLE Students (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    DA VARCHAR(50) NOT NULL,
    Nom VARCHAR(100) NOT NULL,
    Prenom VARCHAR(100) NOT NULL,
    Password VARCHAR(50) NOT NULL,
    AverageGrade DOUBLE NOT NULL,
    Role VARCHAR(50) NOT NULL
);

CREATE TABLE Tutors (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    Nom VARCHAR(100) NOT NULL,
    Prenom VARCHAR(100) NOT NULL,
    DA VARCHAR(50) NOT NULL,
    Password VARCHAR(50) NOT NULL,
    Subject VARCHAR(100) NOT NULL,
    Availability VARCHAR(100) NOT NULL,
    AverageGrade DOUBLE NOT NULL,
    IsValidated BIT NOT NULL,
    Role VARCHAR(50) NOT NULL,
    NumberOfRatings INT NOT NULL DEFAULT 0,
    TotalRatings INT NOT NULL DEFAULT 0
);

CREATE TABLE Reviews (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    TutorId INT,
    Comment VARCHAR(255),
    Rating INT,
    FOREIGN KEY (TutorId) REFERENCES Tutors(Id)
);

CREATE TABLE Appointments (
    Id INT AUTO_INCREMENT PRIMARY KEY,
    StudentId INT,
    TutorId INT,
    DateRDV DATETIME,
    Status VARCHAR(50) DEFAULT 'Pending',
    FOREIGN KEY (StudentId) REFERENCES Students(Id),
    FOREIGN KEY (TutorId) REFERENCES Tutors(Id)
);

-- Fake students (DA fields numeric-only)
INSERT INTO Students (DA, Nom, Prenom, Password, AverageGrade, Role) VALUES
('1001', 'Dupont', 'Alice', 'Password!23', 85.5, 'Student'),
('1002', 'Martin', 'Benoit', 'LetMeIn42', 72.3, 'Student'),
('1003', 'Nguyen', 'Clara', 'Clara2024#', 91.0, 'Student');

-- Fake tutors (DA fields numeric-only)
INSERT INTO Tutors (Nom, Prenom, DA, Password, Subject, Availability, AverageGrade, IsValidated, Role, NumberOfRatings, TotalRatings) VALUES
('Leblanc', 'Sophie', '2001', 'TutorPass1', 'Mathematiques', 'Lundi,Mardi,Jeudi', 88.4, 1, 'Tutor', 10, 44),
('Rousseau', 'Marc', '2002', 'TutorPass2', 'Physique', 'Mercredi,Vendredi', 79.2, 1, 'Tutor', 5, 18),
('Khan', 'Amina', '2003', 'TutorPass3', 'Informatique', 'Mardi,Jeudi', 94.0, 0, 'Tutor', 0, 0);

-- Sample reviews (linked to tutors)
INSERT INTO Reviews (TutorId, Comment, Rating) VALUES
(1, 'Tres clair et patient.', 5),
(1, 'Bonne explication des concepts.', 4),
(2, 'Utile mais parfois presse.', 3);

-- Sample appointments (linked to students and tutors)
INSERT INTO Appointments (StudentId, TutorId, DateRDV, Status) VALUES
(1, 1, '2026-04-15 14:00:00', 'Accepté'),
(2, 2, '2026-05-10 10:30:00', 'Pending'),
(3, 1, '2026-06-01 09:00:00', 'Refusé');

-- Advance AUTO_INCREMENT counters
ALTER TABLE Students AUTO_INCREMENT = 4;
ALTER TABLE Tutors AUTO_INCREMENT = 4;
ALTER TABLE Reviews AUTO_INCREMENT = 4;
ALTER TABLE Appointments AUTO_INCREMENT = 4;